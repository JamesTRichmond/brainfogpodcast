# Ledger

Tracked copy of the living-phases ledger. Same facts as `src/data/ledger.ts`.

Current phase: **7 Cut and Transistor**

See `artifacts/doctrine-graph/LEDGER.md` for the skill load path.
See `/you` for the human clicklist.
